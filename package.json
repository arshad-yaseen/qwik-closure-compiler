{
  "name": "qwik-closure-compiler",
  "module": "src/index.ts",
  "scripts": {
    "demo": "bun run test.ts",
    "test": "bun test",
    "check": "tsc --noEmit && bun test"
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "yuku-analyzer": "^0.5.36"
  }
}
